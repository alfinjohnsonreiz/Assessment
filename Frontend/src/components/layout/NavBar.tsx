import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <div className="w-full border-b border-gray-300 p-4 flex items-center justify-between">
      <h1 className="text-lg font-semibold">Inventory & Billing System </h1>

    </div>
  );
};

export default Navbar;
